// https://www.codewars.com/kata/make-the-deadfish-swim

function parse(sequence) {
  let answerArray = [];
  let total = 0;

  let operations = {
    i : () => {
      total = total + 1;
    },
  }

  return answerArray;
}