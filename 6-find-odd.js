// https://www.codewars.com/kata/54da5a58ea159efa38000836/train/javascript

function findOdd(array) {
  let pairArray = array.map(element => {
    const original = element;
    let originalCounter = 1;

    for (let i=0; i<array.length; i++){
      const current
    }

    return { originalCounter : original}
  });

  return the value of the max key
}

for each element look at every element in the array
compare to index 0.  If element matches index, object gets {element: elementCounter}
return that key:value pair into a big object
return the element value of the largest elementCounter
