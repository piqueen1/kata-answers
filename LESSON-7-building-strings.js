function solution(pairs){
//Kelson: I need to be sure the input is valid
// and have it return pairs whatever letters and however many

  const a = pairs.a;
  const b = pairs.b;

  return `a = ${a},b = ${b}`;
}